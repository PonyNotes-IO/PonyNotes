# 抖音OAuth登录配置指南

## 问题描述

如果您在使用抖音扫码登录时遇到"授权风险提示：当前链接不合法，已限制本次授权"的错误，这是因为回调地址没有在抖音开放平台正确配置。

## 解决方案

### 1. 登录抖音开放平台

访问 [抖音开放平台](https://developer.open-douyin.com/) 并登录您的开发者账号。

### 2. 进入应用管理

1. 登录后，点击"应用管理"
2. 找到您的应用（Client Key: `aws8ujfhmwybxv72`）
3. 点击进入应用详情

### 3. 配置授权回调地址

1. 在应用详情页面，找到"开发管理"
2. 点击"授权管理"
3. 在"授权回调地址"部分，添加以下地址：

**生产环境回调地址：**
```
ponynotes://auth/douyin/callback
```

**开发环境回调地址（可选）：**
```
http://localhost:3000/auth/douyin/callback
```

### 4. 保存配置

添加完回调地址后，点击"保存"按钮。

### 5. 验证配置

配置完成后，重新尝试抖音登录功能。二维码应该可以正常扫码登录了。

## 技术说明

### 回调地址的作用

回调地址是OAuth2.0授权流程中的关键部分：

1. 用户扫码授权后，抖音服务器会将授权码发送到这个回调地址
2. 应用通过这个授权码换取access_token
3. 如果回调地址没有在开放平台配置，抖音会拒绝授权请求

### 自定义URL Scheme

对于移动应用，我们使用自定义URL Scheme `ponynotes://` 来处理回调：

- 这允许抖音应用直接唤起我们的应用
- 避免了Web页面跳转的复杂性
- 提供更好的用户体验

### 开发环境配置

在开发环境中，我们也支持使用 `http://localhost:3000` 作为回调地址，方便本地调试。

## 常见问题

### Q: 为什么不能使用任意回调地址？

A: 这是OAuth2.0的安全机制，防止恶意应用冒充您的应用进行授权。只有预先在开放平台配置的回调地址才会被接受。

### Q: 如果我的域名发生变化怎么办？

A: 您需要在抖音开放平台重新配置新的回调地址，并更新代码中的 `redirectUri` 常量。

### Q: 可以配置多个回调地址吗？

A: 是的，抖音开放平台支持配置多个回调地址，您可以为不同环境（开发、测试、生产）配置不同的地址。

## 联系支持

如果您在配置过程中遇到问题，可以：

1. 查看抖音开放平台的官方文档
2. 联系抖音开放平台的技术支持
3. 检查应用的审核状态（某些功能需要审核通过才能使用）

## 代码位置

相关的代码配置在：
- `frontend/appflowy_flutter/lib/user/application/auth/douyin_auth_service.dart`
- 第39行：`static const String redirectUri = 'ponynotes://auth/douyin/callback';`
